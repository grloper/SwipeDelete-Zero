<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      Base theme is a pitch-black splash/host theme. The full Material 3 Expressive
      OLED palette is applied in Compose (see ui/theme/Theme.kt). We keep the XML
      theme minimal so there is no flash of a light background before Compose draws.
    -->
    <style name="Theme.SwipeDeleteZero" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
    </style>
</resources>
